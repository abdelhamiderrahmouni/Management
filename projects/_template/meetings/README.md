# meetings/

One file per meeting: `YYYY-MM-DD-<topic>.md`.

Suggested shape inside each note: attendees → agenda → notes →
"## Action items" (maintained by the `action-items` skill, each item linked
to its ClickUp task ID).

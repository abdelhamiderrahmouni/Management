# Decision Log — <Project Name>

> Append-only. Never rewrite a past entry's decision, context, or
> consequences; corrections become new entries. The only permitted edit to
> an existing row is advancing its Status. Recorded via the `decision-log`
> skill or by hand in the same shape.

Status values: `Proposed` (direction agreed, final call still open) →
`Accepted` (in force) → `Superseded by #N` (replaced by a later entry).
Reviews and `/focus` flag anything still `Proposed`.

| # | Date | Decision | Context & alternatives | Consequences | Status |
|---|---|---|---|---|---|
| 1 | YYYY-MM-DD | | | | |

---
description: Deep review of one project — reconcile its files against live ClickUp state
agent: project-manager
---
Run the `project-review` skill for the project named in the arguments ($ARGUMENTS) — the folder name under `projects/`.

If no project is named, list the folders in `projects/` and ask which one to review. Finish in the skill's standing format (Needs your attention → Status → Changes I made) and set "Last reviewed" to today in the `WORKSPACE.md` register row.

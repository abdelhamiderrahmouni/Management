---
description: Today's attention report — what needs the user right now
agent: project-manager
---
Produce a fast, attention-first report for right now:

1. Read `WORKSPACE.md` for the portfolio and ClickUp IDs.
2. Query ClickUp across the workspace's projects: overdue tasks, tasks due within 3 days, blocked items, high-priority open work.
3. Check for decisions still marked `Proposed` in each project's `DECISIONS.md` and anything flagged in `PROJECT.md` "Next steps".

Report only: **Needs your attention** (each item one line + recommended action), then a 3-line "on track" summary for everything that doesn't. No file updates unless something is clearly wrong — this is a read-mostly pass.

---
description: What changed in ClickUp since the last check
agent: project-manager
---
Scan ClickUp for what's new or changed across this workspace's projects since the last review:

1. Read `WORKSPACE.md` for the ClickUp IDs and the "Last reviewed" dates.
2. Per project, list: tasks created since last review, status changes, newly assigned items, approaching due dates, and any newly closed work.
3. Anything that contradicts local `PROJECT.md` status — reconcile it.

Report as a compact per-project changelog, then one line: what (if anything) needs a response from the user.
